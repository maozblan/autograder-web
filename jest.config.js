const config = {
    testEnvironment: "jsdom",
    clearMocks: true,
    collectCoverage: true,
    coverageDirectory: "coverage",
    setupFiles: [],
    transform: {},
    verbose: true,
};

export default config;
